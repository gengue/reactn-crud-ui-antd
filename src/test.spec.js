import { ListController, ShowController, FormController } from './';

describe('Antd Components', () => {
  it('ListController is truthy', () => {
    expect(ListController).toBeTruthy();
  });
  it('ShowController is truthy', () => {
    expect(ShowController).toBeTruthy();
  });
  it('FormController is truthy', () => {
    expect(FormController).toBeTruthy();
  });
});
